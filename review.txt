Starting CodeRabbit review in plain text mode...

Connecting to review service
Setting up
Analyzing
Reviewing

============================================================================
File: src/components/projects/ApplicationStatusBanner.tsx
Line: 113 to 119
Type: potential_issue

Comment:
Potential rendering of undefined for waitTime.

When canReapply is false and waitTime is not provided, Line 119 will render "You can apply again in: undefined". Consider adding a fallback or ensuring waitTime is always provided when canReapply is false.




🛡️ Suggested fix

                             ? "You can apply again now"
-                            : You can apply again in: ${waitTime}}
+                            : waitTime
+                                ? You can apply again in: ${waitTime}
+                                : "You cannot reapply at this time"}

Prompt for AI Agent:
In @src/components/projects/ApplicationStatusBanner.tsx around lines 113 - 119, The ternary rendering in ApplicationStatusBanner (variables isRoleFilled, isWithdrawn, canReapply, waitTime) can produce "You can apply again in: undefined" when canReapply is false and waitTime is missing; update the JSX to guard against falsy waitTime (e.g., check waitTime alongside canReapply or provide a fallback value such as "soon" or an empty string) so the branch that renders You can apply again in: ${waitTime} only runs when waitTime is defined, or substitutes a sensible default.



============================================================================
File: src/components/projects/dashboard/TeamCard.tsx
Line: 231 to 265
Type: potential_issue

Comment:
Keyboard handler on overflow avatar doesn't expand the tooltip.

The onKeyDown handler on lines 236-241 prevents default and re-focuses the element, but doesn't actually toggle or show the tooltip. If AvatarGroupTooltip relies on hover, keyboard users won't be able to access the hidden members list.

Verify that AvatarGroupTooltip supports focus-triggered display, or implement a mechanism to show the tooltip on Enter/Space.

Prompt for AI Agent:
In @src/components/projects/dashboard/TeamCard.tsx around lines 231 - 265, The onKeyDown handler on the overflow Avatar currently only prevents default and refocuses without opening the AvatarGroupTooltip; update the keyboard handling so Enter/Space opens the tooltip (or toggle it) and exposes the hidden members to keyboard users: either (A) make AvatarGroupTooltip support focus/keyboard by passing focusable props and ensure it opens on focus/aria-expanded, or (B) control the tooltip via local state (e.g., isTooltipOpen) inside TeamCard and in the Avatar's onKeyDown set isTooltipOpen=true on Enter/Space and toggle on Escape, then pass isTooltipOpen and onClose handlers into AvatarGroupTooltip; also ensure correct ARIA (aria-haspopup/aria-expanded) are set on the Avatar element to reflect the tooltip state.



============================================================================
File: src/components/projects/ApplyRoleModal.tsx
Line: 134 to 148
Type: potential_issue

Comment:
Role selection may be unintentionally reset when roleOptions changes.

This effect runs whenever roleOptions changes while the modal is open. If a user manually selects a role and then roles prop updates (e.g., another user fills a slot), this effect will reset roleId to either the preselected role or the first open role, overwriting the user's selection.

Consider tracking whether the user has manually selected a role:



🛠️ Proposed fix

+    const hasUserSelectedRole = useRef(false);
+
     useEffect(() => {
         if (!isOpen) return;
+        if (hasUserSelectedRole.current) return;
 
         const preselected = preselectedRoleId
             ? roleOptions.find((role) => role.id === preselectedRoleId && !role.disabled)
             : null;
 
         if (preselected) {
             setRoleId(preselected.id);
             return;
         }
 
         const firstOpenRole = roleOptions.find((role) => !role.disabled);
         setRoleId(firstOpenRole?.id || "");
     }, [isOpen, preselectedRoleId, roleOptions]);


Then set hasUserSelectedRole.current = true in the role selection handlers and reset it in resetState.

Prompt for AI Agent:
In @src/components/projects/ApplyRoleModal.tsx around lines 134 - 148, The effect in useEffect that sets roleId when isOpen/preselectedRoleId/roleOptions change resets a user's manual choice; add a ref like hasUserSelectedRole (useRef(false)) and update your role selection handlers (the function(s) that call setRoleId / onChange for role selection) to set hasUserSelectedRole.current = true when the user picks a role, then modify the useEffect to early-return if hasUserSelectedRole.current is true so it won't overwrite a manual selection, and finally clear hasUserSelectedRole.current = false inside resetState (and when the modal closes) to restore default behavior for the next open.



Review completed ✔
